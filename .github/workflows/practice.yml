name: practice-workflow

on:
    push: 
        branches: ["main"]
    workflow_dispatch: 

jobs:
    job1:
        runs-on: ubuntu-22.04

        steps:
            - name: step1
              run: pwd
              
            - name: step2
              run: mvn install  